int removint removefun(int* nums,int numsSize)
{
	//int ret = numsSize;
	//	int i = 0;
	//		for (; i < numsSize - 1; i++)
	//			{
	//					if (*(nums + i) == *(nums + i + 1))
	//							{
	//										int j = 1;
	//													int k = 0;
	//																while (*(nums + i) == *(nums + i + 1 + j) && ((i + 1 + j) < numsSize))
	//																				j++;
	//																							while (1 + j + k < numsSize - i)
	//																										{
	//																														*(nums + i + 1 + k) = *(nums + i + 1 + j + k);
	//																																		k++;
	//																																					}
	//																																								numsSize -= j;
	//																																										}
	//																																											}
	//																																												return numsSize;
	//																																												}
	//
	//																																												int main()
	//																																												{
	//																																													int i = 0;
	//																																														int arr[] = { 1,1 };
	//																																															//int arr[] = {0,0,0,1,2,2,2,34,55,55,55,55,67,67,68,89,100,100,101};
	//																																																int len = sizeof(arr) / sizeof(arr[0]);
	//																																																	len = removefun(arr,len);
	//																																																		for (i = 0; i < len; i++)
	//																																																			{
	//																																																					printf("%d ", arr[i]);
	//																																																						}
	//																																																							return 0;
	//																																int removefun(int* nums, int numsSize)
	//																																{
	//																																	//int ret = numsSize;
	//																																		int i = 0;
	//																																			for (; i < numsSize - 1; i++)
	//																																				{
	//																																						if (*(nums + i) == *(nums + i + 1))
	//																																								{
	//																																											int j = 1;
	//																																														int k = 0;
	//																																																	while (*(nums + i) == *(nums + i + 1 + j) && ((i + 1 + j) < numsSize))
	//																																																					j++;
	//																																																								while (1 + j + k < numsSize - i)
	//																																																											{
	//																																																															*(nums + i + 1 + k) = *(nums + i + 1 + j + k);
	//																																																																			k++;
	//																																																																						}
	//																																																																									numsSize -= j;
	//																																																																											}
	//																																																																												}
	//																																																																													return numsSize;
	//																																																																													}
	//
	//																																																																													int main()
	//																																																																													{
	//																																																																														int i = 0;
	//																																																																															int arr[] = { 1,1 };
	//																																																																																//int arr[] = {0,0,0,1,2,2,2,34,55,55,55,55,67,67,68,89,100,100,101};
	//																																																																																	int len = sizeof(arr) / sizeof(arr[0]);
	//																																																																																		len = removefun(arr,len);
	//																																																																																			for (i = 0; i < len; i++)
	//																																																																																				{
	//																																																																																						printf("%d ", arr[i]);
	//																																																																																							}
	//																																																																																								return 0;
	//																																																																	int removefun(int* nums, int numsSize)
	//																																																																	{
	//																																																																		//int ret = numsSize;
	//																																																																			int i = 0;
	//																																																																				for (; i < numsSize - 1; i++)
	//																																																																					{
	//																																																																							if (*(nums + i) == *(nums + i + 1))
	//																																																																									{
	//																																																																												int j = 1;
	//																																																																															int k = 0;
	//																																																																																		while (*(nums + i) == *(nums + i + 1 + j) && ((i + 1 + j) < numsSize))
	//																																																																																						j++;
	//																																																																																									while (1 + j + k < numsSize - i)
	//																																																																																												{
	//																																																																																																*(nums + i + 1 + k) = *(nums + i + 1 + j + k);
	//																																																																																																				k++;
	//																																																																																																							}
	//																																																																																																										numsSize -= j;
	//																																																																																																												}
	//																																																																																																													}
	//																																																																																																														return numsSize;
	//																																																																																																														}
	//
	//																																																																																																														int main()
	//																																																																																																														{
	//																																																																																																															int i = 0;
	//																																																																																																																int arr[] = { 1,1 };
	//																																																																																																																	//int arr[] = {0,0,0,1,2,2,2,34,55,55,55,55,67,67,68,89,100,100,101};
	//																																																																																																																		int len = sizeof(arr) / sizeof(arr[0]);
	//																																																																																																																			len = removefun(arr,len);
	//																																																																																																																				for (i = 0; i < len; i++)
	//																																																																																																																					{
	//																																																																																																																							printf("%d ", arr[i]);
	//																																																																																																																								}
	//																																																																																																																									return 0;
	//																																																																																																																									}Size)
{
	//int ret = numsSize;
	//	int i = 0;
	//		for (; i < numsSize - 1; i++)
	//			{
	//					if (*(nums + i) == *(nums + i + 1))
	//							{
	//										int j = 1;
	//													int k = 0;
	//																while (*(nums + i) == *(nums + i + 1 + j) && (																																																}}
